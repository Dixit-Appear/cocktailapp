   <div class="navbar-lewagon">
      <div class="navbar-lewagon-actions d-none d-sm-flex">
        <%= link_to "All cocktails", cocktails_path, class: "btn btn-ghost"  %>
      </div>
      <div class="navbar-lewagon-actions d-none d-sm-flex">
        <%= link_to "Home", root_path, class: "btn btn-ghost"  %>
      </div>
      <div class="badge-container navbar-lewagon-actions d-none d-sm-flex">
        <%= form_tag cocktails_path, method: :get do %>
          <%= text_field_tag :query,
            params[:query],
            class: "form-control",
            placeholder: "Find a cocktail"
          %>
        <% end %>
        <%= link_to "Add Cocktail", new_cocktail_path, class: "btn btn-ghost" %>
      </div>
      <!-- displayed on phone / tablet -->
    </div>

